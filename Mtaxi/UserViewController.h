/*
 
 
lembretes
 
 
 1 - falar com eduardo sobre o if que adicionei na classe marshaller... pode gerar efeito colateral..
 
 2 - continuam os erros apos o update do driver info e do passenger info... o update funciona, mas posteriormente o usuário e senha ficam invalidos.
 
 3 - precisamos adicionar o código para autenticar o usuário automaticamente quando receber o erro 403.
 
 

 
 
 */

#import <UIKit/UIKit.h>

@interface UserViewController : UITableViewController

@end
